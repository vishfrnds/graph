# this runs nano gpt model with single character encoding
# and single character decoding, though we can change this with predifined maps

import torch
import nanoGPT.data.shakespeare_char.prepare as prepare
